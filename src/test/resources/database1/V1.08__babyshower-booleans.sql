
SET FOREIGN_KEY_CHECKS = 0;
DROP TABLE `events`;
ALTER TABLE `babyshower` ADD COLUMN `is_at_private_home` bit(1) NULL DEFAULT NULL AFTER `has_address`, ADD COLUMN `notification_one_month_sent` bit(1) NOT NULL DEFAULT b'0' AFTER `address`, ADD COLUMN `notification_one_week_sent` bit(1) NOT NULL DEFAULT b'0' AFTER `notification_one_month_sent`, ADD COLUMN `notification_two_weeks_sent` bit(1) NOT NULL DEFAULT b'0' AFTER `notification_one_week_sent`, ADD COLUMN `notification_day_before_sent` bit(1) NOT NULL DEFAULT b'0' AFTER `notification_two_weeks_sent`, ADD COLUMN `notification_on_the_day_sent` bit(1) NOT NULL DEFAULT b'0' AFTER `notification_day_before_sent`, ADD COLUMN `has_gifts` bit(1) NULL DEFAULT NULL AFTER `notification_on_the_day_sent`, ADD COLUMN `has_party_favors` bit(1) NULL DEFAULT NULL AFTER `has_gifts`, ADD COLUMN `has_games` bit(1) NULL DEFAULT NULL AFTER `has_party_favors`, ADD COLUMN `has_physical_invitations` bit(1) NULL DEFAULT NULL AFTER `has_games`, ADD COLUMN `has_cake` bit(1) NULL DEFAULT NULL AFTER `has_physical_invitations`, ADD COLUMN `has_flowers` bit(1) NULL DEFAULT NULL AFTER `has_cake`, ADD COLUMN `has_balloons` bit(1) NULL DEFAULT NULL AFTER `has_flowers`, ADD COLUMN `has_decorations` bit(1) NULL DEFAULT NULL AFTER `has_balloons`, ADD COLUMN `has_photographer` bit(1) NULL DEFAULT NULL AFTER `has_decorations`, ADD COLUMN `has_food` bit(1) NULL DEFAULT NULL AFTER `has_photographer`, ADD COLUMN `has_thankyou_cards` bit(1) NULL DEFAULT NULL AFTER `has_food`, ADD COLUMN `has_gift_for_mom` bit(1) NULL DEFAULT NULL AFTER `has_thankyou_cards`, ADD COLUMN `has_gift_for_baby` bit(1) NULL DEFAULT NULL AFTER `has_gift_for_mom`, ADD COLUMN `latitude` decimal(10,8) NULL DEFAULT NULL AFTER `has_gift_for_baby`, ADD COLUMN `longitude` decimal(11,8) NULL DEFAULT NULL AFTER `latitude`, MODIFY COLUMN `hostname` varchar(64) NOT NULL AFTER `deleted_date`, MODIFY COLUMN `name_of_party` varchar(64) NOT NULL AFTER `hostname`, MODIFY COLUMN `date_of_party` bigint(20) NULL DEFAULT NULL AFTER `name_of_party`, MODIFY COLUMN `time_of_party` int(11) NULL DEFAULT NULL AFTER `date_of_party`, MODIFY COLUMN `theme` varchar(64) NULL DEFAULT NULL AFTER `time_of_party`, MODIFY COLUMN `name_of_baby` varchar(64) NULL DEFAULT NULL AFTER `theme`, MODIFY COLUMN `dob_baby` bigint(20) NULL DEFAULT NULL AFTER `name_of_baby`, MODIFY COLUMN `name_of_mom` varchar(64) NULL DEFAULT NULL AFTER `dob_baby`;
SET FOREIGN_KEY_CHECKS = 1;
